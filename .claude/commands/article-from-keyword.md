## /article-from-keyword — 키워드가 이미 있을 때

사용자가 제공한 키워드로 Step 4(TopicAnalyzer)부터 시작.

1. 사용자에게 CJ Stage 확인
2. TopicAnalyzer: SERP 분석 → 주제 랭킹 → Content Brief → HTML 여부
3. ContentWriter → ContentGapAnalyzer → SourceVerifier → PersonaReviewer → SeoScorer → QualityChecker
